# include<bits/stdc++.h>
using namespace std;
int main()
{
   string str1,str2="";
   cin>>str1;
   
   ength()-1);i>-1;i--)
   {
       str2=str2+str1[i];
   }
  
   if(str1==str2)
   cout<<"string is palindrom";
   else 
   cout<<"not palindrom";
   return 0; 
}
